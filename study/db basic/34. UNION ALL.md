* UNION ALL
- union all: union without removal duplicates
- union: removal duplicates

* practice guide: consider performance
- union all is faster than union
- if need to remove duplicates, use union
- else use union all
  - know exactly that two sets has no duplicates
  - duplicates are not important

