each table attribute contains only a single, atomic value

```sql
create table orders_1nf_violating(
    order_id        int not null,
    member_id       int not null,
    member_name     varchar(50) not null,
    product_infos   varchar(200) not null, -- non atomic
    ordered_at      datetime not null
)
```

```sql
create table orders_1nf(
    order_id        int not null,
    member_id       int not null,
    member_name     varchar(50) not null,
    product_id      int not null, -- atomic
    product_name    varchar(50) not null, -- atomic
    product_price   int not null,
    order_price     int not null,
    order_quantity  int not null,
    ordered_at      datetime not null,
    primary key (order_id, product_id)
)
```