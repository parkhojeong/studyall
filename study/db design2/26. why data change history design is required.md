```sql
use my_shop4;
-- ### 이력이 없는 원본 테이블
DROP TABLE IF EXISTS product;

CREATE TABLE product (
    product_id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(200) NOT NULL,
    price INT NOT NULL,
    stock_quantity INT NOT NULL DEFAULT 0,
    status VARCHAR(20) NOT NULL DEFAULT 'ACTIVE'
);

INSERT INTO product (name, price, stock_quantity, status) VALUES 
('스마트폰 케이스', 15000, 100, 'ACTIVE'),
('무선 이어폰', 89000, 50, 'ACTIVE'),
('노트북 파우치', 35000, 30, 'ACTIVE');

select * from product;
```
in this table, these questions cannot be answered.
1. who does it change?
2. when is it changed?
3. why is it changed?
4. what is the change?

when a table is updated, existing data is erased 
