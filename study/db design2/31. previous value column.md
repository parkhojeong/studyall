simple idea: store current value and previous value
- price: current price
- previous_price
- price_changed_at
```sql
create table product (
    product_id bigint primary key auto_increment,
    price int not null,
    previous_price int,
    price_changed_at datetime
) 
```
- problem: only single previous value can be saved

* previous value column - summary
pros
- simple implementation
- useful only single previous value
cons
- only a single previous value 
when to use
- when update is rare
- when previous value is enough

* SCD Type 3
- SCD(slowly changing dimension): how to manage data that changes slowly
- Type3: previous value column