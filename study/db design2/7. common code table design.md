* common code minimum factors
- code value
- display name
```sql
create table common_code(
    code varchar(50) primary key,
    name varchar(50) not null
);

INSERT INTO common_code (code, name) VALUES
('ORDER', '주문접수'),
('PAID', '결제완료'),
('SHIPPING', '배송중'),
('DELIVERED', '배송완료'),
('CANCEL', '주문취소');
```

- requirement: member grade is managed by common code
```sql
INSERT INTO common_code (code, name) VALUES
('NORMAL', '일반회원'),
('VIP', 'VIP회원'),
('VVIP', 'VVIP회원'); 
```

problem: code value could be duplicated