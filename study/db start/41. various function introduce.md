* various function introduce
- SQL standard function 
  - String Functions
    - UPPER(string)
    - LOWER(string)
    - SUBSTRING()
    - TRIM()
    - POSITION()
    - CHAR_LENGTH()/CHARACTER_LENGTH()
  - Numeric Functions
    - ABS(number)
    - MOD(dividend, divisor)
    - ROUND(number, decimals)
    - CEILING(number)
    - FLOOR(number)
    - SQRT(number)
    - POWER(base, exponent)
  - Date and Time Functions
    - CURRENT_DATE
    - CURRENT_TIME
    - CURRENT_TIMESTAMP
    - EXTRACT(field FROM source)
  - Conditional and Conversion Functions
    - CAST(expression AS data_type)
    - COALESCE(value1, value2, ...)
    - NULLIF(expression1, expression2)
  - Aggregate Functions
    - AVG(expression)
    - COUNT(expression)
    - COUNT(*)
    - MAX(expression)
    - MIN(expression)
    - SUM(expression)

database dialect
- MySQL functions
- docs: https://dev.mysql.com/doc/refman/8.0/en/functions.html
  - String Functions
    - CONCAT(str1, str2, ...)
    - CONCAT_WS(separator, str1, ...)
    - GROUP_CONCAT(expression)
    - ...
  - Date and Time Functions
    - NOW()
    - SYSDATE()
    - DATE_FORMAT(date, format)
    - ...
  - Numeric Functions
    - RAND([seed])
    - ...
  - Flow Control Functions
    - IF(condition, value_if_true, value_if_false)
    - IFNULL(expression1, expression2)
  - Windows Functions - MySQL 8.0+
    - ROW_NUMBER() OVER(PARTITION BY ...)
    - ...
  - JSON Functions - MySQL 5.7+
    - JSON_OBJECT(key1, val1, ...)
    - ...
  - OTHERS
    - UUID()
    - ...
    - 