* NULL - un known value
- null is unknown value. special value
- NULL comparison 
  - IS NULL
  - IS NOT NULL
- MySQL NULL ordering
  - deal with the smallest value
  - Oracle deal with the most positive value

# how to order NULL first, description DESC
- (description IS NULL) DESC,
```sql
select * -- , description IS NULL -- can omit 
from products
ORDER BY (description IS NULL) DESC, description DESC;
```