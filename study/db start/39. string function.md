* string function
- why it's nessary
  - show result as '이순신 (yisunsin@example.com)'
- CONCAT: string concatanation
  - CONCAT(string1, string2, ...) 
  - ex. SELECT CONCAT(name, ' (', email, ')')
- string basic function
  - CONCAT_WS(separator, string1, string2, ...) - MySQL function
    - ex. SELECT CONCAT_WS(' - ', name, email, address)
  - UPPER(string), LOWER(string)
  - LENGTH(string): byte length
  - CHAR_LENGTH(string): character length