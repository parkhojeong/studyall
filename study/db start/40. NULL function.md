* NULL function
- requirement: handle NULL to "description is empty"
- function handling NULL - MySQL
  - IFNULL(expression1, expression2)
    - return expression1 if expression1 is not NULL, else expression2
  - COALESCE(expression1, expression2, ...)
    - return first non-NULL value, else NULL
    - more flexible than IFNULL