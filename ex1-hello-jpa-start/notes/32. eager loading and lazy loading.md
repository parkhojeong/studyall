* lazy loading
- when a field is accessed, it is loaded

* eager loading
- always load, even if not accessed
- in jpql, n+1 problem can occur
- @ManyToOne, @OneToOne default is eager

* practice guide
- use lazy loading default
- if join is required, use fetch join or entity graph