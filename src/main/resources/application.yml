spring:
  application:
    name: studyall
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_shop?&serverTimezone=Asia/Seoul
    username: root
    password: test1234
  sql:
    init:
      mode: always
      schema-locations: classpath:db_basic_schema.sql
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    org.springframework.jdbc.datasource.init: debug
